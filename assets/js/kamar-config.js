// Kamar config placeholder. On production, fill these values or define window.KAMAR_SUPABASE_URL and window.KAMAR_SUPABASE_ANON_KEY before this file loads.
window.KAMAR_CONFIG = window.KAMAR_CONFIG || {
  supabaseUrl: window.KAMAR_SUPABASE_URL || '',
  supabaseAnonKey: window.KAMAR_SUPABASE_ANON_KEY || ''
};
